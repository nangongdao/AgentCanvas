{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the AgentCanvas shell. The UI talks to the sidecar over HTTP, so the shell itself needs no plugin permissions yet.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
